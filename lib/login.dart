improt'package:flutter/material.dart';

class LoginPage extends StatelessWidget {
  const LoginPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    appBar:AppBar(
    title:Text("Login page"),
      centerTitle true,
    ),
    body:padding()
    child:Column(),
    mainAxisAligment
    TextField()
    decoration: InputDecoration()
    labelText:"Home",
    );
    }

}
